# Copy to .env and fill your keys.
OPENAI_API_KEY=sk-...            # required for Stage 2 classification
SCALESERP_API_KEY=...            # required for Stage 1 search

# Optional overrides (leave blank to use defaults from config.py)
#TEMP_PDF_DIR=./temp_pdfs
#CONFIRMED_PDF_DIR=./confirmed_pdfs
